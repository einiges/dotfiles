#!/usr/bin/env sh

if [ $# -ne 1 ]; then
	exit 1
fi

uri="$1"

# remove protocol
uri="${uri#*://}"

# remove path
uri="${uri%%/*}"

# remove user info
uri="${uri#*@}"

# remove port
uri="${uri%%:}"

printf "%s\n" "$uri"

