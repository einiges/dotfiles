#!/usr/bin/env sh

[ $# -lt 1 ] && exit 1

case ${1} in
	*north ) d=south ;;
	*east  ) d=west  ;;
	*south ) d=north ;;
	*west  ) d=east  ;;
	cancel ) d=cancel ;;
	* ) exit 1 ;;
esac

[ ${1:0:1} = '~' ] && d="~${d}"

bspc node -p "$d"
